/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.imagin.studio','cars-by-api-ninjas.p.rapidapi.com'
        ]
    }
}

module.exports = nextConfig
